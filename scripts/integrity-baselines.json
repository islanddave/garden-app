{
  "seeded_at": "2026-07-28",
  "seeded_by": "W3.D (fourthread V102) from fourthread-evidence-20260728.md W0.2-r1 rows + W3.D dry-run for checked-edge classes",
  "repo_home_when_shipped": "garden-app/scripts/integrity-baselines.json",
  "evidence_run_ids": {
    "event_unattached_total": "DEMOTED TO INFO-ONLY 2026-08-03 (BUG-EVTCASCADE-001 triage) — removed from alert_metrics, value refreshed 53 -> 63 as a trend anchor only. It is a CUMULATIVE census of a shipped intentional path: project-level (plant_id NULL) harvest/photo logging, the rows V4-HARVESTQTY-001 surfaces as 'unattributed' and events/index.js harvest-summary calls 'a live daily inflow'. All 10 rows of the 2026-08-03 breach were legitimate project-scoped events. A cumulative counter of normal behavior can only ratchet up, so as an alert it demanded a weekly re-baseline forever. Alerting moved to event_unattached_new_7d",
    "event_unattached_new_7d": "NEW 2026-08-03 — replaces event_unattached_total in alert_metrics. THRESHOLD, not a census: 7-day inflow of unattached events. Trailing-8-week observed inflow 2,4,5,5,4,5,11 (wk of 07-27 = 11, harvest-season peak); measured 10 at seeding. Set to 25, ~2x the observed peak, so ordinary harvest-season variation is silent while a genuine regression (a write path that stops attributing plant_id, which would produce dozens/day) still fires. Self-clearing — no re-baseline needed after a spike passes",
    "photos_parentless": "BUG-PHOTOPARENT-001 resolved 2026-07-29: the W0.2-r1 count of 6 was a checker false positive — all 6 had live inventory_item_id parents (seed-packet photos, inventory/{item}/ paths); predicate now counts inventory_item_id as a parent; corrected live count 0. RECURRENCE OF THE SAME CLASS 2026-08-02 (V4-SPACEPHOTO-001): space_id was likewise absent, so the first space photo would have paged an ALERT for a legal row; predicate now counts space_id too. THIRD RECURRENCE PRE-EMPTED 2026-08-12 (W-INTEG, ships with W-DEL): the predicate enumerated the CHECK's six parent clauses but not its SEVENTH, the pending_tag escape — so the first quick-tag photo left un-tagged (or a W-BATCHNULL fallback row) would have paged an ALERT for a row the CHECK explicitly permits; predicate now carries AND intake_status IS DISTINCT FROM 'pending_tag'. RULE, restated unambiguously and superseding the earlier wording: this predicate must enumerate EVERY clause of photos_must_have_parent, INCLUDING the pending_tag escape — a parentless row that is NOT pending is still a genuine violation and is still counted. Live count under both the old and new predicate on 2026-08-12: 0 (0 pending_tag rows live), so the baseline is unchanged and the fix is pre-emptive, not a re-baseline",
    "photos_to_deleted_inventory": "added 2026-07-29 with the BUG-PHOTOPARENT-001 fix — same orphan-edge class as photos_to_deleted_events for the fifth parent type; measured live 0",
    "phantom_photo_events": "W0.2-r1 phantom-count (22; forensic verdict W0.2b-r1 = pre-fix debris)",
    "care_dupe_groups": "W0.2-r1 care-dupe-groups (1314)",
    "care_dupe_excess": "RE-BASELINED 1442 -> 1607 on 2026-08-14 (garden-bdwrap-20260814). Growth is the DESIGNED consequence of V4-PLANTMERGE-001 deliberately NOT collapsing same-day water rows: three specialists refuted the collapse's premise before any prod write (zero water_depth light/deep rows in 10,114, and it bucketed UTC against an ET ledger), so merged winners legitimately now hold multiple same-day water rows where two duplicate plantings each held one. Removing the collapse preserved ~198 rows of real history. Re-baselined AFTER all 14 groups merged (group 14 Echeveria ran 2026-08-14), so this value covers the complete merge, not a partial one.",
    "harvest_multi_groups": "W0.2-r1 care-dupe-groups (37, legitimate/exempt — info only)",
    "user_stats_drift": "W0.2-r1 user-stats-drift (1: user_3CxBE... 275 ledger XP, no user_stats row)",
    "s3_not_in_db": "W0.2-r1 s3-db-mismatch (6)",
    "db_not_in_s3": "W0.2-r1 s3-db-mismatch (0)",
    "harvest_log_to_deleted_events": "4 -> 0 2026-08-03 (BUG-EVTCASCADE-001). ROOT CAUSE: DELETE /api/events/:id and the batch undo both soft-deleted event_log ONLY — no child cascade — so 18 of 45 all-time deletes stranded children. Fix ships in the same commit: harvest_log now cascade-soft-deletes with its event. The 9 live orphans were repaired by migrations/v4-evtcascade-001 (data-only). Baseline is 0 and must STAY 0 — any growth is a real regression of the cascade, not drift",
    "photos_to_deleted_events": "5 -> 0 2026-08-03 (BUG-EVTCASCADE-001, same root cause as harvest_log_to_deleted_events). Photos DETACH rather than cascade — a photo is irreplaceable user content, so the undo nulls event_id and re-parents to the event's project/plant instead of deleting the picture. All 6 live orphans already carried a project_id and stayed visible in the gallery; repaired by migrations/v4-evtcascade-001. Baseline 0 and must stay 0",
    "critter_state_orphans": "9 -> 0 2026-08-03 — PREDICATE NARROWED, no data repaired. Was counting soft-deleted parents, i.e. deliberate policy: rewards are never clawed back when a source event is undone or a planting deleted (the events Lambda states the same rule for XP/streak/achievements), so the metric could only ratchet upward and 10 of 10 live rows were this benign case. Now counts MISSING (hard-deleted / never-existent) parents only — measured live 0 at the narrowing. That is genuine referential damage and must stay 0",
    "cultivars_untyped": "HARD-ZERO, superseding croptype-population-plan-V102 §5, which specified this metric as 'info-only, not hard-zero' because the 2 then-untyped live cultivars (Lantana, Neon Rose) were ornamentals Dave owns and a hard-zero metric 'would ship permanently red'. Both were resolved between 2026-08-03 and 2026-08-05: Lantana -> crop type 'lantana' (minted), Neon Rose -> 'calibrachoa' (botanically correct; the plan predicted 'rose'). Verified live 2026-08-05. The population is genuinely 0, so the plan's reason for info-only no longer applies. NAMING: the plan calls this cultivars_untyped_total — it is registered here as cultivars_untyped; the croptype-populate-20260803 thread should adopt this name rather than mint a second metric for the same predicate. Its OWN §5 metrics (croptype_underpopulated_new_7d / _total) remain unbuilt and in that thread's scope. || NEW 2026-08-05 (V4-INTAKE-001, L-239). Live cultivars with crop_type_slug IS NULL — the 'Unsorted' class, which NOTHING detected before this metric: check-cultivar-faceting.mjs filters to crop_type_slug IS NOT NULL and healthcheck-cultivar-facets.mjs derives desired tags from the row, so a null slug produces no desired type tag and no gap. Both reported OK on exactly the broken population. Measured live 0 on prod 2026-08-05 (407 live cultivars, 203 of them created in the prior 30 days) with this exact predicate. Baseline 0 and must STAY 0 — any growth is a live intake regression, not accumulated debris. NOT reconciler-fixable: applyDerive cannot invent a crop type, so a hit means the WRITE SITE omitted it (known live candidate: /admin/classify POSTs /api/varieties with only name + source_proj_rescope_project_id)",
    "cultivars_missing_type_tag": "NEW 2026-08-05 (V4-INTAKE-001, L-239). Live cultivars that HAVE a crop_type_slug but no derived type: tag link — a write that reached plant_varieties without the applyDerive tail (direct-Neon insert, or a loader that skipped the heal). Predicate reproduced verbatim from scripts/check-cultivar-faceting.mjs, which existed but was scheduled NOWHERE (no GHA workflow, no package.json script, no claude-ops task referenced it) — this metric is what makes it actually run. Measured live 0 on prod 2026-08-05. Baseline 0 and must STAY 0. Remediate with scripts/reconcile-cultivar-facets.mjs",
    "checked_edges": "W3.D-r1 dry-run 2026-07-28 (audit-plan V100 point-in-time values were 39/5/4/4/1/4; measured tonight 39/5/4/9/1/4 — critter_state=9 because the shipped definition covers BOTH edges: source_event_id->deleted-event OR plant_id->deleted-plant, broader than the audit's unspecified-edge 4)",
    "events_to_deleted_plants": "RE-BASELINED 39 -> 56 on 2026-08-14 (garden-bdwrap-20260814) on an explicit Dave ruling, NOT a repair. The +17 is Curly Parsley (soft-deleted 2026-08-07). Dave ruled the events STAY: 'assume those were real events that happened even if I kept the planting open longer than it was really being worked ... let's keep it.' This RESOLVES the policy switch V4-SOFTDEL-001 F3 deliberately deferred: HIDE_EVENTS_UNDER_DELETED_PLANTING (lambda/events/index.js) STAYS false — a soft-deleted planting retracts the RECORD, not the history of waterings and harvests that really happened in that container. 0 of the 56 carry a harvest_log row, so no harvest total is affected either way. This metric is therefore a CENSUS of an accepted state, not a defect counter: it will keep ratcheting up by design on every future planting soft-delete, and re-baselining it is the expected maintenance, not an alert being silenced. Ledger V4-PLANTDELCASCADE-001 closed as not-a-bug on this ruling.",
    "entity_memory_orphans": "UNCHANGED at 4, and that is the point: the Curly Parsley plant_id orphan (which took the composite 4 -> 5 on 2026-08-07) was cleared 2026-08-14 after confirming it was a PRE-FIX FOSSIL — BUG-CACHEORPHANLEAK-001's forward fix shipped 2026-08-10, AFTER that delete, so the fix has no hole. Cache rows are an explicit hard-delete carve-out of the Soft-Delete-Only Rule (regenerable from event_log). Pre-state: project-state/entity-memory-orphan-BEFORE-20260814.csv. The remaining 4 are project_id orphans, not plant_id ones — this metric counts BOTH parents."
  },
  "alert_metrics": [
    "event_unattached_new_7d",
    "events_to_deleted_plants",
    "photos_to_deleted_events",
    "harvest_log_to_deleted_events",
    "critter_state_orphans",
    "favorites_orphans",
    "entity_memory_orphans",
    "photos_parentless",
    "photos_to_deleted_inventory",
    "care_dupe_excess",
    "phantom_photo_events",
    "user_stats_drift",
    "s3_not_in_db",
    "db_not_in_s3",
    "cultivars_untyped",
    "cultivars_missing_type_tag"
  ],
  "threshold_metrics": [
    "event_unattached_new_7d",
    "cultivars_untyped",
    "cultivars_missing_type_tag"
  ],
  "metrics": {
    "event_unattached_total": 63,
    "event_unattached_new_7d": 25,
    "events_to_deleted_plants": 56,
    "photos_to_deleted_events": 0,
    "harvest_log_to_deleted_events": 0,
    "critter_state_orphans": 0,
    "favorites_orphans": 1,
    "entity_memory_orphans": 4,
    "photos_parentless": 0,
    "photos_to_deleted_inventory": 0,
    "care_dupe_groups": 1314,
    "care_dupe_excess": 1607,
    "harvest_multi_groups": 37,
    "phantom_photo_events": 22,
    "user_stats_drift": 1,
    "s3_not_in_db": 6,
    "db_not_in_s3": 0,
    "cultivars_untyped": 0,
    "cultivars_missing_type_tag": 0
  },
  "rebaselined_at": "2026-08-14",
  "rebaselined_by": "garden-bdwrap-20260814 — Dave ruling on V4-SOFTDEL-001 F3 + post-merge steady state. S3 metrics NOT touched (UNMEASURED this run, PHOTOS_BUCKET unset)."
}