# Touch (edit + push to dev) to fire .github/workflows/revert-rehearsal.yml.
# Run pauses at environment:production for Dave's approval before any staging mutation.
MODE=dump_path
TARGET_VERSION=v0.0.0
PREREVERT_VERSION=v0.0.1
