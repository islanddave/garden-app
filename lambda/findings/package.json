{
  "name": "garden-lambda-findings",
  "version": "1.0.0",
  "type": "module",
  "description": "DRG Care Knowledge Engine V1 — findings read model. engine/ = pure deterministic engine (slices 2-5). index.js = GET-only read handler (slice 6). evidence-ingest (slice 7) is a separate lambda dir.",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "@clerk/backend": "^1.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0"
  }
}
